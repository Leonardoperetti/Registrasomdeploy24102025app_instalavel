# Variáveis de Ambiente para o Projeto RegistraSom

# Chave de API da OpenAI (necessária para a funcionalidade de transcrição)
OPENAI_API_KEY="SUA_CHAVE_AQUI"

# Configurações do Banco de Dados (SQLite é o padrão, mas pode ser mudado para PostgreSQL ou MongoDB)
# SQLALCHEMY_DATABASE_URI="sqlite:///registrasom.db"

# Porta de acesso ao Frontend (padrão 3000)
FRONTEND_PORT=3000

# Porta de acesso ao Backend (padrão 8000)
BACKEND_PORT=8000

